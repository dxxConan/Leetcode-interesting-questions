Think this question as find the largest area of histogram. But we need to do this for every row. every row should be regraded as the bottom of a histogram.
And to find the largest area of histogram, we should use a data structure call increasing stack